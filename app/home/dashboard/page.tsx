import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dashboard | TMEDIA',
};

const page = () => {
  return (
    <main>
      <h1>Dashboard Page</h1>
    </main>
  );
};

export default page;
